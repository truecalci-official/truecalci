{
  "name": "truecalci",
  "version": "2.6.1",
  "description": "Deterministic statutory & financial compute engine for AI agents and enterprise teams over the Model Context Protocol (MCP).",
  "type": "module",
  "main": "mcp-server.mjs",
  "bin": {
    "truecalci-mcp": "./mcp-server.mjs"
  },
  "scripts": {
    "test": "node scripts/audit_registries_and_links.mjs && node tests/test_suite.mjs",
    "audit:links": "node scripts/audit_registries_and_links.mjs",
    "sync": "node scripts/sync_public_deploy.mjs",
    "preflight": "node scripts/sync_public_deploy.mjs && node scripts/audit_registries_and_links.mjs && node tests/test_suite.mjs",
    "deploy": "npm run preflight && wrangler deploy",
    "start": "node server.mjs",
    "mcp": "node mcp-server.mjs"
  },
  "keywords": [
    "mcp",
    "modelcontextprotocol",
    "claude",
    "cursor",
    "deterministic-compute",
    "statutory-tax",
    "financial-engine",
    "cloudflare-workers"
  ],
  "author": "TrueCalci Team",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
